export interface WorkExperience {
  logo: string;
  company: string;
  role: string;
  date: string;
}

export interface Project {
  logo: string;
  title: string;
  description: string;
  url: string;
}
